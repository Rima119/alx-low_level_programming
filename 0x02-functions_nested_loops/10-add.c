#include <stdio.h>
#include "main.h"

/**
 * add -  a function that adds two integers and returns the result.
 * @n: int type number
 * @m: int type number
 * Return: Addition result
 */

int add(int n, int m)
{
	return (n + m);
}
